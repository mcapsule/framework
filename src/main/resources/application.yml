spring:
  application:
    name: framework
  data:
    jdbc:
      dialect: maria
app:
  postgres:
    url: jdbc:mariadb://localhost:3306/mariadb
    username: root
    password: root
    testWhileIdle: true
    validationQuery: SELECT 1
  mariadb:
    url: jdbc:mariadb://localhost:3306/mariadb
    username: root
    password: root
    testWhileIdle: true
    validationQuery: SELECT 1